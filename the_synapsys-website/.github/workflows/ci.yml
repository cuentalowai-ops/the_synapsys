name: CI - Website

on:
  push:
    branches: [main, develop]
    paths:
      - 'the_synapsys-website/**'
      - '.github/workflows/ci-website.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'the_synapsys-website/**'

env:
  NODE_VERSION: '20.x'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'the_synapsys-website/package-lock.json'
      
      - name: Install dependencies
        working-directory: the_synapsys-website
        run: npm ci
      
      - name: Run linter
        working-directory: the_synapsys-website
        run: npm run lint
      
      - name: Check code formatting
        working-directory: the_synapsys-website
        run: npm run format:check
      
      - name: Build project
        working-directory: the_synapsys-website
        run: npm run build
      
      - name: Run tests
        working-directory: the_synapsys-website
        run: npm test
